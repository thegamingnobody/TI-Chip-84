# TI-Chip-84

To build, download the [C/C++ CE toolchain](https://ce-programming.github.io/toolchain/static/getting-started.html)
Make sure to add the `CEdev/bin` folder to the PATH enviroment.

Once installed, open the project folder (the folder with the makefile) in a command window and run the `make` command.
